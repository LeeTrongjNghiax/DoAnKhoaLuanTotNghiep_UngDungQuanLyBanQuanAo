import { FindFirstImagePipe } from './find-first-image.pipe';

describe('FindFirstImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FindFirstImagePipe();
    expect(pipe).toBeTruthy();
  });
});
