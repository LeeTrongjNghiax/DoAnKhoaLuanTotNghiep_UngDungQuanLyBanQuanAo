import { FindAbstractPipe } from './find-abstract.pipe';

describe('FindAbstractPipe', () => {
  it('create an instance', () => {
    const pipe = new FindAbstractPipe();
    expect(pipe).toBeTruthy();
  });
});
